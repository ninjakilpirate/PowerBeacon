# services/redis/redis.conf
bind 0.0.0.0
protected-mode no

# persistence (good for an offline lab so data survives restarts)
appendonly yes
appendfsync everysec

# optional: if you decide to use a password later, uncomment and set a value
#requirepass password
